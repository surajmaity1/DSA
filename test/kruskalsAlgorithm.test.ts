import { describe, it, expect } from "vitest";
import { insert } from "../Graph/kruskalsAlgorithm.ts";

describe("Kruskal’s algorithm", () => {
  describe("Kruskal’s algorithm", () => {
    const graph = insert();
  });
});
