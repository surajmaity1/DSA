### To make given array to heap or complete binary tree to heap, it'll take:
- O(n) + c

#### Reason:
- For evey time, height will not be O (log(n))


#### Picture of creating heap:
![alt text](images/image.png)