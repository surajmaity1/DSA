import type { Graph } from "../../../Graph/graph.ts";

export const generateMST = (graph: Graph) => {
  const size = graph.adjacencyList.size;
  const visited: boolean[] = Array(size).fill(false);
  const finalMST = [];

  
};
